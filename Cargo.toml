[workspace]

members = [
      "binary-search",
      "quick-sort",
      "tests",
      "back-track-maze",
]
          
[profile.release]
strip = true
opt-level = "s"
lto = true
codegen-units = 1
#panic = "abort"
